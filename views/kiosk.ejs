<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>Kiosk</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
    body { 
      font-family: Arial; 
      text-align: center; 
      padding: 20px; 
      background: #fff; 
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }
    .wrapper {
      width: 100%;
      max-width: 1200px;
    }
    img { width: 100px; margin: 20px auto; display: block; }
    h1 { font-size: clamp(1.5rem, 5vw, 2.5rem); margin: 20px; }
    .badge { 
      background: #fff; 
      color: #000; 
      padding: 10px 20px; 
      border-radius: 20px; 
      display: inline-block; 
      margin: 20px; 
      border: 2px solid #000;
      font-size: clamp(0.9rem, 3vw, 1.1rem);
    }
    .btns { 
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(min(250px, 100%), 1fr));
      gap: 20px; 
      margin: 40px auto; 
      padding: 0 10px;
      max-width: 100%;
    }
    .btn { 
      padding: clamp(30px, 8vw, 50px); 
      border: 4px solid; 
      border-radius: 20px; 
      cursor: pointer; 
      font-size: clamp(1rem, 3vw, 1.3rem);
      background: white;
      transition: all 0.3s;
      touch-action: manipulation;
      -webkit-user-select: none;
      user-select: none;
    }
    .btn:active {
      transform: scale(0.95);
    }
    .btn:hover { 
      transform: translateY(-5px); 
      box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }
    .green { background: #e8f5e9; border-color: #4CAF50; }
    .yellow { background: #fff9c4; border-color: #FFC107; }
    .red { background: #ffebee; border-color: #f44336; }
    .emoji { 
      font-size: clamp(60px, 15vw, 90px); 
      display: block; 
      margin-bottom: 15px; 
    }
    
    @media (max-width: 768px) {
      body { padding: 10px; }
      img { width: 80px; margin: 15px auto; }
      h1 { margin: 15px; }
      .badge { padding: 8px 15px; margin: 15px; }
      .btns { gap: 15px; margin: 20px auto; }
    }
    
    @media (max-width: 480px) {
      img { width: 60px; margin: 10px auto; }
      .btns { gap: 10px; margin: 15px auto; }
      .btn { padding: 25px; border-width: 3px; }
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <img src="https://upload.wikimedia.org/wikipedia/commons/9/90/Mercedes-Logo.svg">
    <h1>Jak oceniasz wizytę w serwisie?</h1>
    <div class="badge"><%= name %></div>
    <div class="btns">
      <button class="btn green" onclick="go('happy')">
        <span class="emoji">😊</span>
        Bardzo dobrze
      </button>
      <button class="btn yellow" onclick="go('neutral')">
        <span class="emoji">😐</span>
        W porządku
      </button>
      <button class="btn red" onclick="go('sad')">
        <span class="emoji">☹️</span>
        Źle
      </button>
    </div>
  </div>
  
  <script>
    // Włącz fullscreen przy załadowaniu
    window.onload = function() {
      setTimeout(enterFullscreen, 500);
    };
    
    function enterFullscreen() {
      const elem = document.documentElement;
      
      if (elem.requestFullscreen) {
        elem.requestFullscreen();
      } else if (elem.webkitRequestFullscreen) { // Safari
        elem.webkitRequestFullscreen();
      } else if (elem.mozRequestFullScreen) { // Firefox
        elem.mozRequestFullScreen();
      } else if (elem.msRequestFullscreen) { // IE/Edge
        elem.msRequestFullscreen();
      }
    }
    
    function go(rating) {
      location.href = '/thanks?loc=<%= loc %>&rating=' + rating;
    }
  </script>
</body>
</html>

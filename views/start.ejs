<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>Wybierz lokalizację</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
    body { 
      font-family: Arial; 
      background: #ffffff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .container {
      text-align: center;
      width: 100%;
      max-width: 1200px;
    }
    .logo {
      width: clamp(80px, 15vw, 150px);
      margin-bottom: clamp(20px, 5vw, 30px);
    }
    h1 {
      color: #333;
      font-size: clamp(1.5rem, 5vw, 3rem);
      margin-bottom: clamp(10px, 3vw, 20px);
    }
    h2 {
      color: #666;
      font-size: clamp(1rem, 3vw, 1.5rem);
      margin-bottom: clamp(30px, 6vw, 50px);
      font-weight: normal;
    }
    
    .map-container {
      position: relative;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    
    .map-svg {
      width: 100%;
      height: auto;
      filter: drop-shadow(0 4px 10px rgba(0,0,0,0.1));
    }
    
    .location-marker {
      cursor: pointer;
      transition: all 0.3s;
    }
    
    .location-marker:hover circle {
      r: 18;
      fill: #45a049;
    }
    
    .location-marker:active {
      transform: scale(0.9);
    }
    
    .location-marker text {
      pointer-events: none;
      font-weight: bold;
      font-size: 14px;
    }
    
    .admin-link {
      position: fixed;
      bottom: clamp(15px, 3vw, 30px);
      right: clamp(15px, 3vw, 30px);
      background: #4CAF50;
      color: white;
      padding: clamp(10px, 2vw, 15px) clamp(20px, 4vw, 30px);
      border-radius: 30px;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      font-size: clamp(0.8rem, 2.5vw, 1rem);
      z-index: 1000;
    }
    .admin-link:hover {
      background: #45a049;
      transform: scale(1.05);
    }
    
    @media (max-width: 768px) {
      body { padding: 15px; }
      .map-container { padding: 10px; }
      .location-marker text { font-size: 12px; }
    }
    
    @media (max-width: 480px) {
      body { padding: 10px; }
      .location-marker text { font-size: 10px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <img src="https://upload.wikimedia.org/wikipedia/commons/9/90/Mercedes-Logo.svg" alt="Mercedes" class="logo">
    <h1>Daimler Truck Retail Polska</h1>
    <h2>Wybierz lokalizację kiosku na mapie:</h2>
    
    <div class="map-container">
      <svg class="map-svg" viewBox="0 0 400 500" xmlns="http://www.w3.org/2000/svg">
        <!-- Mapa Polski (uproszczona) -->
        <path d="M 200 50 L 350 100 L 380 200 L 370 300 L 350 400 L 300 450 L 200 470 L 100 450 L 50 400 L 30 300 L 20 200 L 50 100 Z" 
              fill="#f0f0f0" 
              stroke="#333" 
              stroke-width="2"/>
        
        <!-- Ruda Śląska (południowy zachód) -->
        <g class="location-marker" onclick="openKiosk('ruda')">
          <circle cx="120" cy="380" r="15" fill="#4CAF50"/>
          <circle cx="120" cy="380" r="8" fill="white"/>
          <text x="120" y="410" text-anchor="middle" fill="#333">Ruda Śląska</text>
        </g>
        
        <!-- Siedlce (wschód-centrum) -->
        <g class="location-marker" onclick="openKiosk('siedlce')">
          <circle cx="320" cy="220" r="15" fill="#4CAF50"/>
          <circle cx="320" cy="220" r="8" fill="white"/>
          <text x="320" y="250" text-anchor="middle" fill="#333">Siedlce</text>
        </g>
        
        <!-- Emilianów (centrum) -->
        <g class="location-marker" onclick="openKiosk('emilianow')">
          <circle cx="200" cy="250" r="15" fill="#4CAF50"/>
          <circle cx="200" cy="250" r="8" fill="white"/>
          <text x="200" y="280" text-anchor="middle" fill="#333">Emilianów</text>
        </g>
      </svg>
    </div>
  </div>
  
  <a href="/admin/login" class="admin-link">🔐 Panel Admina</a>
  
  <script>
    function openKiosk(location) {
      window.location.href = '/' + location;
    }
  </script>
</body>
</html>

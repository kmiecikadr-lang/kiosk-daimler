<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>Wybierz lokalizację</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
    body { 
      font-family: Arial; 
      background: #ffffff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .container {
      text-align: center;
      width: 100%;
      max-width: 1000px;
    }
    .logo {
      width: clamp(80px, 15vw, 150px);
      margin-bottom: clamp(20px, 5vw, 30px);
    }
    h1 {
      color: #333;
      font-size: clamp(1.5rem, 5vw, 3rem);
      margin-bottom: clamp(10px, 3vw, 20px);
    }
    h2 {
      color: #666;
      font-size: clamp(1rem, 3vw, 1.5rem);
      margin-bottom: clamp(30px, 6vw, 50px);
      font-weight: normal;
    }
    
    .map-wrapper {
      position: relative;
      max-width: 700px;
      margin: 0 auto;
    }
    
    .map-image {
      width: 100%;
      height: auto;
      display: block;
    }
    
    .location-pin {
      position: absolute;
      width: 50px;
      height: 50px;
      background: #4CAF50;
      border: 4px solid white;
      border-radius: 50%;
      cursor: pointer;
      transform: translate(-50%, -50%);
      transition: all 0.3s;
      box-shadow: 0 6px 15px rgba(0,0,0,0.4);
      z-index: 10;
    }
    
    .location-pin:hover {
      transform: translate(-50%, -50%) scale(1.3);
      background: #45a049;
      box-shadow: 0 8px 20px rgba(0,0,0,0.5);
    }
    
    .location-pin:active {
      transform: translate(-50%, -50%) scale(0.95);
    }
    
    .location-label {
      position: absolute;
      transform: translate(-50%, 15px);
      background: rgba(0,0,0,0.85);
      color: white;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: clamp(13px, 3vw, 18px);
      font-weight: bold;
      white-space: nowrap;
      pointer-events: none;
      z-index: 5;
    }
    
    /* Pozycje zgodne z Twoją mapą */
    #pin-ruda { top: 68%; left: 35%; }
    #label-ruda { top: 68%; left: 35%; }
    
    #pin-siedlce { top: 44%; left: 74%; }
    #label-siedlce { top: 44%; left: 74%; }
    
    #pin-emilianow { top: 36%; left: 56%; }
    #label-emilianow { top: 36%; left: 56%; }
    
    .admin-link {
      position: fixed;
      bottom: clamp(15px, 3vw, 30px);
      right: clamp(15px, 3vw, 30px);
      background: #4CAF50;
      color: white;
      padding: clamp(10px, 2vw, 15px) clamp(20px, 4vw, 30px);
      border-radius: 30px;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      font-size: clamp(0.8rem, 2.5vw, 1rem);
      z-index: 1000;
    }
    .admin-link:hover {
      background: #45a049;
      transform: scale(1.05);
    }
    
    @media (max-width: 768px) {
      body { padding: 15px; }
      .location-pin { width: 40px; height: 40px; }
    }
    
    @media (max-width: 480px) {
      body { padding: 10px; }
      .location-pin { width: 35px; height: 35px; border-width: 3px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <img src="https://upload.wikimedia.org/wikipedia/commons/9/90/Mercedes-Logo.svg" alt="Mercedes" class="logo">
    <h1>Daimler Truck Retail Polska</h1>
    <h2>Wybierz lokalizację kiosku na mapie:</h2>
    
    <div class="map-wrapper">
      <img src="/mapa-polski.png" alt="Mapa Polski" class="map-image">
      
      <div id="pin-ruda" class="location-pin" onclick="openKiosk('ruda')"></div>
      <div id="label-ruda" class="location-label">Ruda Śląska</div>
      
      <div id="pin-siedlce" class="location-pin" onclick="openKiosk('siedlce')"></div>
      <div id="label-siedlce" class="location-label">Siedlce</div>
      
      <div id="pin-emilianow" class="location-pin" onclick="openKiosk('emilianow')"></div>
      <div id="label-emilianow" class="location-label">Emilianów</div>
    </div>
  </div>
  
  <a href="/admin/login" class="admin-link">🔐 Panel Admina</a>
  
  <script>
    function openKiosk(location) {
      window.location.href = '/' + location;
    }
  </script>
</body>
</html>

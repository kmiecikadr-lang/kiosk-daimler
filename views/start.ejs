<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>Wybierz lokalizację</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
    body { 
      font-family: Arial; 
      background: #ffffff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .container {
      text-align: center;
      width: 100%;
      max-width: 1000px;
    }
    .logo {
      width: clamp(80px, 15vw, 150px);
      margin-bottom: clamp(20px, 5vw, 30px);
    }
    h1 {
      color: #333;
      font-size: clamp(1.5rem, 5vw, 3rem);
      margin-bottom: clamp(10px, 3vw, 20px);
    }
    h2 {
      color: #666;
      font-size: clamp(1rem, 3vw, 1.5rem);
      margin-bottom: clamp(30px, 6vw, 50px);
      font-weight: normal;
    }
    
    .map-wrapper {
      position: relative;
      max-width: 700px;
      margin: 0 auto;
      cursor: pointer;
    }
    
    .map-image {
      width: 100%;
      height: auto;
      display: block;
    }
    
    .clickable-area {
      position: absolute;
      width: 35px;
      height: 35px;
      border-radius: 50%;
      cursor: pointer;
      transform: translate(-50%, -50%);
      z-index: 10;
    }
    
    .clickable-area:hover {
      background: rgba(76, 175, 80, 0.4);
      width: 45px;
      height: 45px;
    }
    
    .clickable-area:active {
      background: rgba(76, 175, 80, 0.6);
    }
    
    #area-emilianow { top: 30%; left: 59%; }
    #area-siedlce { top: 38.5%; left: 67%; }
    #area-ruda { top: 61.5%; left: 40%; }
    
    .admin-link {
      position: fixed;
      bottom: clamp(15px, 3vw, 30px);
      right: clamp(15px, 3vw, 30px);
      background: #4CAF50;
      color: white;
      padding: clamp(10px, 2vw, 15px) clamp(20px, 4vw, 30px);
      border-radius: 30px;
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      font-size: clamp(0.8rem, 2.5vw, 1rem);
      z-index: 1000;
    }
    .admin-link:hover {
      background: #45a049;
      transform: scale(1.05);
    }
    
    @media (max-width: 768px) {
      body { padding: 15px; }
      .clickable-area { width: 45px; height: 45px; }
      .clickable-area:hover { width: 55px; height: 55px; }
    }
    
    @media (max-width: 480px) {
      body { padding: 10px; }
      .clickable-area { width: 50px; height: 50px; }
      .clickable-area:hover { width: 60px; height: 60px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <img src="https://upload.wikimedia.org/wikipedia/commons/9/90/Mercedes-Logo.svg" alt="Mercedes" class="logo">
    <h1>Daimler Truck Retail Polska</h1>
    <h2>Wybierz lokalizację kiosku na mapie:</h2>
    
    <div class="map-wrapper">
      <img src="/mapa-polski.png" alt="Mapa Polski" class="map-image">
      
      <div id="area-emilianow" class="clickable-area" onclick="openKiosk('emilianow')"></div>
      <div id="area-siedlce" class="clickable-area" onclick="openKiosk('siedlce')"></div>
      <div id="area-ruda" class="clickable-area" onclick="openKiosk('ruda')"></div>
    </div>
  </div>
  
  <a href="/admin/login" class="admin-link">🔐 Panel Admina</a>
  
  <script>
    function openKiosk(location) {
      window.location.href = '/' + location;
    }
  </script>
</body>
</html>
